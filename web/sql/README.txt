This folder contains two scripts related to the installation of the ICNML database.

The 'run_preinstall.sh' script has to be run first to create the ICNML database and the roles (icnml user).

The 'run_install.sh' script run all the SQL files in the ./install folder, to install all tables, views and sequences.
